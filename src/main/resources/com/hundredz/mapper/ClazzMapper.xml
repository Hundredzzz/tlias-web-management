<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hundredz.mapper.ClazzMapper">
    <insert id="add">
        insert into clazz(name,room ,begin_date,end_date,master_id,subject,create_time,update_time) values(#{name},#{room},#{beginDate},#{endDate},#{masterId},#{subject},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update clazz set name=#{name},room=#{room},
                         begin_date=#{beginDate},
                         end_date=#{endDate},
                         master_id=#{masterId},
                         subject=#{subject},
                         update_time=#{updateTime} where id=#{id}
    </update>
    <delete id="deleteById">
        delete from clazz where id = #{id}
    </delete>

    <select id="list" resultType="com.hundredz.pojo.Clazz">
        select c.*, e.name masterName from clazz c left join emp e on c.master_id = e.id
        <where>
            <if test="name !=null and name !=''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin !=null and end !=null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>
        order by update_time desc
    </select>
    <select id="selectById" resultType="com.hundredz.pojo.Clazz">
        select c.*, e.name masterName
        from clazz c left join emp e on c.master_id = e.id
        where c.id = #{id}
    </select>
    <select id="findAll" resultType="com.hundredz.pojo.Clazz">
        select * from clazz
    </select>
    <select id="countStudentByClazzId" resultType="java.lang.Integer">
        select count(*) from student where clazz_id = #{id}
    </select>
</mapper>